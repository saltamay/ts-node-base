{
  "watch": ["src/**/*.ts", "index.ts"],
  "ext": "ts,js,json",
  "ignore": ["node_modules", "coverage", "dist"],
  "exec": "npx ts-node -r tsconfig-paths/register ./index.ts",
  "restartable": "rs",
  "env": {
    "NODE_ENV": "development"
  }
}
